---
import { Page_connect } from '@cogov/ui-server'
import { ctx_ } from '@ctx-core/object'
import Layout from '../../layouts/Layout.astro'
const ctx = ctx_()
---

<Layout ctx={ctx}>
	<Page_connect ctx={ctx}/>
</Layout>
